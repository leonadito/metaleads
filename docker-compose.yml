services:
  web:
    build: .
    restart: always
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./secrets:/app/secrets:ro
      - ./staticfiles:/app/staticfiles
